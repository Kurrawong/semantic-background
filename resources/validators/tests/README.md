# Validator Tests

These tests - pytest files starting `text_...` in subdirs - can be run directly with Python or indirectly with pytest.

When running with Python all tests and test files are tested and passes and fails are all printed.

_not implemented yet_
When running with pytest, assertions are checked, so execution will fail after one error.
