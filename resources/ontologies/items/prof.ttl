PREFIX : <http://www.w3.org/ns/dx/prof/>
PREFIX dc: <http://purl.org/dc/elements/1.1/>
PREFIX dct: <http://purl.org/dc/terms/>
PREFIX ont: <http://www.w3.org/ns/dx/prof>
PREFIX owl: <http://www.w3.org/2002/07/owl#>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX schema: <https://schema.org/>
PREFIX skos: <http://www.w3.org/2004/02/skos/core#>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>

:ResourceRole
    a owl:Class ;
    rdfs:isDefinedBy ont: ;
    rdfs:subClassOf skos:Concept ;
    skos:usageNote "Specific terms must come from a vocabulary" ;
    schema:description "A role that an profile resource, described by a Resource Descriptor, plays" ;
    schema:name "Resource Role" ;
.

:hasArtifact
    a owl:ObjectProperty ;
    rdfs:domain :ResourceDescriptor ;
    rdfs:isDefinedBy ont: ;
    skos:usageNote "A property to link from a Resource Descriptor to an actual information resource (rdfs:Resource; an individual) that implements it" ;
    schema:description "The URL of a downloadable file with particulars such as its format and role indicated by the Resource Descriptor" ;
    schema:name "has artifact" ;
.

:hasResource
    a owl:ObjectProperty ;
    rdfs:isDefinedBy ont: ;
    rdfs:range :ResourceDescriptor ;
    skos:usageNote "This property is analogous to dcat:distribution however the domain is not necessarily dcat:Dataset. dcat:Dataset and prof:Profile are not disjoint, so it is up to implementations to decide if these are equivalent properties" ;
    schema:description "A resource which describes the nature of an artifact and the role it plays in relation to the Profile" ;
    schema:name "has resource" ;
.

:hasRole
    a owl:ObjectProperty ;
    rdfs:domain :ResourceDescriptor ;
    rdfs:isDefinedBy ont: ;
    rdfs:range skos:Concept ;
    skos:usageNote "A set of common roles are defined by the Profiles Vocabulary. These are not exhaustive or disjoint, and may be extended for situations where finer grained description of purpose is necessary. A resource may perform multiple roles" ;
    schema:description "The function of an artifact described by a Resource Descriptor, such as specification, guidance etc." ;
    schema:name "has role" ;
.

:hasToken
    a owl:DatatypeProperty ;
    rdfs:domain :Profile ;
    rdfs:isDefinedBy ont: ;
    rdfs:range xsd:token ;
    skos:usageNote "A simple lexical form of identifier that may be accepted in some circumstances, such as API arguments to reference this profile. This is the “preferred term”, since alternative identifiers may be declared and used by any implementation" ;
    schema:description "The preferred identifier for the Profile, for use in circumstances where its URI cannot be used" ;
    schema:name "has token" ;
.

:isInheritedFrom
    a owl:ObjectProperty ;
    rdfs:domain :ResourceDescriptor ;
    rdfs:isDefinedBy ont: ;
    rdfs:range :Profile ;
    skos:usageNote "This property is created for the convenience of clients. When profile describers wish to allow clients to discover all resources relevant to a Profile without having to navigating an inheritance hierarchy of prof:profileOf relations, this predicate may be used to directly associate inherited Profile Descriptors with the Profile. If this property is present, it should be used consistently and all relevant resources a client may need to utilise the profile should be present and described using this predicate" ;
    schema:description "A base specification, a Resource Descriptor from which is to be considered a Resource Descriptor for this Profile also" ;
    schema:name "is inherited from" ;
.

:isProfileOf
    a owl:ObjectProperty ;
    rdfs:domain :Profile ;
    rdfs:isDefinedBy ont: ;
    rdfs:range dct:Standard ;
    rdfs:subPropertyOf :isTransitiveProfileOf ;
    skos:usageNote "A Profile may define constraints on the usage of one or more specifications. All constraints of these specifications are inherited, in the sense that an object conforming to a profile conforms to all the constraints specified the targets of prof:isProfileOf relations. This property is optional, allowing any specification to be declared at the root of a profile hierarchy using the Profile class" ;
    schema:description "A specification for which this Profile defines constraints, extensions, or which it uses in combination with other specifications, or provides guidance or explanation about its usage" ;
    schema:name "is profile of" ;
.

:isTransitiveProfileOf
    a owl:ObjectProperty ;
    rdfs:domain :Profile ;
    rdfs:isDefinedBy ont: ;
    rdfs:range dct:Standard ;
    skos:usageNote "This is a convenience property that may be used to access all specifications (including other profiles) that could provide useful information and related resources for the Profile (for example, for better identifying conformance requirements). This avoids forcing clients to traverse a profile hierarchy to find all relevant resources. If this property is used, then all such relationships should be present so a client can safely avoid hierarchy traversal" ;
    schema:description "The transitive closure of the prof:isProfileOf property. Relates a profile to another specification that it is a profile of, possibly via a chain of intermediate profiles that are in prof:isProfileOf relationships" ;
    schema:name "is transitive profile of" ;
.

:Profile
    a owl:Class ;
    dct:source "https://www.w3.org/2017/dxwg/wiki/ProfileContext"^^xsd:anyURI ;
    rdfs:isDefinedBy ont: ;
    rdfs:subClassOf dct:Standard ;
    schema:description """A specification that constrains, extends, combines, or provides guidance or explanation about the usage of other specifications.

This definition includes what are often called "application profiles", "metadata application profiles", or "metadata profiles".""" ;
    schema:name "Profile" ;
.

:ResourceDescriptor
    a owl:Class ;
    rdfs:isDefinedBy ont: ;
    skos:usageNote "Can be use to indicate the formalism (via dct:format) and any adherence to a dct:Standard (via dct:conformsTo) to allow for machine mediation as well as its purpose via relation to a ResourceRole (via hasRole)" ;
    schema:description "A description of a resource that defines an aspect - a particular part, feature or role - of a Profile" ;
    schema:name "Resource Descriptor" ;
.

ont:
    a owl:Ontology ;
    skos:historyNote "2026-01 NJC: added OntPub elements, including the Ontology class definition" ;
    dc:contributor
        "Alejandra Gonzalez-Beltran" ,
        "Antoine Isaac" ,
        "Makx Dekkers" ,
        "Simon Cox" ;
    owl:versionIRI :1.0 ;
    owl:versionInfo "1.0" ;
    skos:historyNote "2026-01 NJC: added OntPub elements, including the Ontology class definition" ;
    schema:creator
        <https://orcid.org/0000-0002-7878-2693> ,
        <https://orcid.org/0000-0002-8742-7730> ;
    schema:dateCreated "2018-02-16"^^xsd:date ;
    schema:dateIssued "2019-12-18"^^xsd:date ;
    schema:dateModified "2019-10-25"^^xsd:date ;
    schema:description """This vocabulary is for describing relationships between standards/specifications, profiles of them and supporting artifacts such as validating resources.

This model starts with [http://dublincore.org/2012/06/14/dcterms#Standard](dct:Standard) entities which can either be Base Specifications (a standard not profiling any other Standard) or Profiles (Standards which do profile others). Base Specifications or Profiles can have Resource Descriptors associated with them that defines implementing rules for the it. Resource Descriptors must indicate the role they play (to guide, to validate etc.) and the formalism they adhere to (dct:format) to allow for content negotiation. A vocabulary of Resource Roles are provided alongside this vocabulary but that list is extensible.""" ;
    schema:name "Profiles Vocabulary" ;
    schema:publisher <https://www.w3.org> ;
.
